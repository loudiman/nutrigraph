# NutriGraph

## Agent skills

### Issue tracker

Issues live in GitHub Issues on `loudiman/nutrigraph`. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: `CONTEXT.md` and `docs/adr/` at the repository root. Neither exists yet; create them lazily.

## Wayfinder

The planning effort is charted as a map: issue #1, labelled `wayfinder:map`. Read the map before you choose a ticket.
